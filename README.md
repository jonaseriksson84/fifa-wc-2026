# FIFA WC 2026 Prediction Pool

## Getting started

Run `wrangler d1 create fifa-wc-2026` to create your D1 database, paste the `database_id` into `wrangler.toml`, then `npm install && npm run db:migrate && npm run seed:fixtures && npm run dev`.
